
const defaults  = {
  list:[],
  preset:['Work','Prep','Meet'],
  weeklyHours: 32,
  user:'teacher',
  delPreset:false,
  mode:'Total',
  showSettings:false,
  counter: {
    active: false,
    start: null,
    comment: '' }
};

export default defaults;
